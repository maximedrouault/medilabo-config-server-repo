server:
    port: 8080

management:
    endpoints:
        web:
            exposure:
                include: "health"
    tracing:
        enabled: true
    zipkin:
        enabled: true
        endpoint: http://localhost:9411/api/v2/spans
        tracing:
            sampler:
                probability: 1.0

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/

patient-api.url: http://localhost:9000