server:
  port: 8761

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: false
    fetch-registry: false
  environment: prod

management:
  endpoints:
    web:
      exposure:
        include: "health"